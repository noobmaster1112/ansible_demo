---

- hosts: all
  become: true
  tasks:

  - name: update repo index and install apache and php package
    package:
      name: 
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes
